# STATUS — kb-init

> 最后更新：2026-08-17（发布面备齐：LICENSE / 元数据 / CI 配置 / 首跑体验；未公开、未发包）

## 当前阶段

**Plan 1 已合并 main**（6 轮 Codex 终审，93 passed，Notion 死链 2/237）。
**Plan 2 已拆成五个子项目**（2A 索引 → 2B L2 洞察 → 2D compile → 2C Wrapped → 2E L3）。
**2A 索引层**（165 passed）、**2B 洞察层**（含前置 2A′，299 passed + 4 smoke，5 轮 Codex 终审）
、**2D compile** 与 **2C Wrapped**（471 passed + 6 smoke）已实现完毕，
spec 与计划见 `docs/superpowers/`。

**README 承诺的两样门面产物都有了**：`kb-init compile` 产出的 `CLAUDE.md`，
以及 `report.private.html` / `report.share.html`。两份真实语料上都跑通，
产物在仓库外 `~/Documents/assistant-output/kb-init-r2/`。
**DESIGN §4.2 的闭环到此才真正接上**——在 2C 之前，人肉 gate 只有一份枯燥的
配置文件，而那正是这份设计当初判定行不通的东西。

2A 期间两个改变设计的实测发现：
1. **日期几乎不存在**——导出类语料的日期可解析率仅 5–6%（已维护库 43%），根因是导出包里就没有。
   DESIGN §5 的 L2 四类洞察里三类依赖时间轴，故降级为条件洞察：覆盖率低于 30% 自动关闭。
   **L2 在目标语料上的重心从「轨迹」移到了「主题」。**
2. **模型不是聚类质量的瓶颈**（R2 验收，见 DESIGN §13）——7× 体积只换来 3 个百分点的未归类率。
   真正影响观感的是聚类方法与分块粒度。

原始阶段记录： 这是"系统开源第一块"计划的落地项目——目标是扩大在 Agent 领域的影响力。

## 最近进展

- 2026-08-13 完成需求收敛，7 轮讨论定完全部核心分叉，设计文档 `docs/DESIGN.md` v0.1 落盘
- 关键收敛路径：Memory vs KB 三层拆分 → 冷启动是真痛点 → 载体定为独立 CLI → 从结构诊断转向内容洞察 → 双输出（Wrapped + CLAUDE.md）确立验收闭环
- 竞品检索确认三块空位：冷启动批量导入 / 内容洞察 L2-L3 / CLAUDE.md 生成
- **B 段（KB 管线打包）已砍**：`AgriciDaniel/claude-obsidian` 10.8k★ 已占且更完整
- 实测地面真值已获取（`probes/kb_doctor_probe.py`）：Notion 导出 1925 篇 60% 空壳，Apple Notes 620→242 留存 39%

- 2026-08-13 Codex 5.6-Sol 工程 review 完成（commit `0f411d3`）：裁决 R2/R9，抓到流水线顺序真 bug，补 §4.3 IR 合同，新增 R11–R15

## 卡住的

无阻塞项。

- **R14 已定案**（2026-08-13 贾老师拍板）：v0.1 只走 `uvx`，**不同时维护两套实现**；宣传语不吹"零安装"，改为"装好 uv 后一条命令运行，无需自己装 Python"；首次运行体验按一等公民设计（进度 / 预估时间 / 模型下载可见）；GitHub Releases 单二进制列入 **v0.2 明确路线**

## 历史里程碑

1. ~~验证 R1~~ ✅ 2026-08-13，clone 源码逐条核实，三条断言全部成立（见 DESIGN §2.3）
2. ~~Plan 1 核心管线~~ ✅ 2026-08-14 合并 main
3. ~~R2 聚类质量验收~~ ✅ 2026-08-14，结论见 DESIGN §13
4. ~~2A 索引层~~ ✅ 2026-08-15 实现完毕
5. ~~2A′ 过大簇细分~~ ✅ 2026-08-15（回头条件②触发后的处置）
6. ~~2B L2 洞察层 + insights 合同~~ ✅ 2026-08-15
7. ~~2D `compile` → CLAUDE.md~~ ✅ 2026-08-16（头号分叉裁为「纯管道」）
8. ~~2C Wrapped 报告~~ ✅ 2026-08-16（DESIGN §4.2 的闭环到此接上）

## 语料资产（已在手）

| 用途 | 路径 |
|---|---|
| 主校准集（烂语料，60% 空壳） | `~/Documents/notion-export/Export-1b76b367-.../` |
| 副校准集（40% 空壳） | `~/Documents/Obsidian Vault/Archive/Apple Notes` |
| 处理后配对样本（验证编译效果） | `~/Documents/Obsidian Vault/Personal/*/Apple Notes/` |
| 优等生对照组 | `~/Documents/Obsidian Vault/Wiki/` |
| 当年的转换脚本（参考） | `~/Documents/assistant-output/notion_to_obsidian.py` |


## 阻断项修复轮（2026-08-13）

Codex 5.6-Sol 全分支终审判定「暂不合并」，6 条阻断项 + 5 条必修 minor 已全部处理：

| 项 | 状态 |
|---|---|
| B1 路径冻结逐篇而非全局 | ✅ emit 重构为两遍式 + 引用映射 + unresolved 记账 |
| B2 文件系统等价名碰撞 | ✅ 入口 NFC+casefold 检测记账 / 出口等价键唯一化 |
| B3 目录输入无总量限制 | ✅ 累计 .md 字节执行 max_total_bytes |
| B4 单文件原子≠整次运行原子 | ✅ staging 目录整体发布 |
| B5 文件名注入 frontmatter | ✅ 改用 yaml.safe_dump |
| B6 CLI 无错误边界 | ✅ 退出码合同 0/1/2/3/4，不吐 traceback |
| 5 条必修 minor | ✅ null byte / 超大文件静默跳过 / frontmatter 炸弹 / 年份回归 / .md.tmp 残骸 |

**最关键的发现**：B1 修完后合成测试全绿，但真实语料 288 条内部链接 **100% 死链**——
Notion 导出用的是 URL 编码的标准相对链接而非 wikilink，只处理 wikilink 会漏掉全部真实链接。
补 `_rewrite_md_links` 后降到 2/227（0.9%，均为嵌套结构链接）。
> ⚠️ 这个「2/227」后来被证明是低估——当时的计数漏掉了文件名含 `#` 的一整类。
> 同口径复量的真实基线是 15/240，见下一节。
**合成语料测不出真实形态，这是这一轮最贵的教训。**

测试 67 → 78 passed。

## 链接层终审收口（2026-08-14，第 4–6 轮）

前三轮修完仍有一条「活着的错链」：`_rewrite_md_links` 没有当前文档的目录上下文。
四审、五审各又抓出同一家族的绕过路径，六审裁定可合并。三轮改动都在 `emit.py` 一个文件里：

| 轮 | 问题 | 修法 |
|---|---|---|
| 3 审遗留 | 标准相对链接按 basename/stem 跨目录兜底 | 新建 `by_path` 索引，`_LinkIndex` 把「名字语义」(wikilink) 与「路径语义」(标准链接) 分成两张表 |
| 4 审 | 「当前目录不中就退回导出根」仍在猜基准 | 删掉根回退，链接基准唯一 = 当前文档所在目录（CommonMark） |
| 4 审 | `--wikilinks` 整体跳过解析，歧义作废失效、slug 化输出名点不开 | 方言模式同样先解析，只改输出语法 |
| 4 审 | 文件名含 `#` 时被当锚点切掉 | 按每个 `#` 位置枚举候选，由 `by_path` 实际命中裁决 |
| 5 审 | 精确别名已判歧义，剥 `.md` 回退又把它解析掉 | 别名解析改三态 `_resolve_alias → (name, ambiguous)`，歧义即停 |

**贯穿这三轮的同一个教训**：每一条都是「解析不到就再换一种猜法」的变体。
只要还留着一条兜底路径，「歧义不猜」就会被它绕过——而错链不像死链，不会有人来报。

真实语料上的净效果：Notion 内部链接死链 15 → 2（剩余 2 条是嵌套结构链接，已记入 README 已知限制）。
其中 11 条来自一类此前从未被计入的缺陷：文件名含 `#`。**旧账面「死链 2/227」是低估**——
当时那把尺子漏数了这一类。测试 80 → 93 passed。

## 2B 与回头条件的处置（2026-08-15）

**回头条件②在第二份语料上触发了**：Notion 757 篇只聚出 2 个 group，其中一个 509 篇（67%），
内容横跨界面组件 / 设计史 / 思想史 / 投资，不可命名。

2A spec 原写"任一触发即加 halo"，该映射对①②是错配的——halo 提高覆盖率但不产生新主题。
处置为 **2A′ 过大簇细分**：

- 检测器用**语料自校准**的判据（簇内聚度相对本语料 residual 基线的提升量），
  不用篇数比例魔数。实测 6 个正常簇 +0.18…+0.23，巨簇 +0.069，阈值 0.12 落在空档里。
- 细分为 `analyses[1]`（复用 2A 已留的 `parent_analysis_id` / `input_scope`），
  **主分析的方法与参数完全不变**；子簇逐个过同一检测器，不过的折回 residual。
- 实测：Notion 巨簇细分出 9 个子簇 9/9 通过 → 主题 2 → 10；
  Apple Notes 无 group 触发检测器，**零改动**。

**条件③判为 `not_evaluable`**：它要求"第二份第三方语料"，而手上两份语料同属一位作者。
不拿第二份自有语料冒充第三方语料来宣布它通过。halo 保持未裁决，
判定结果随产物落盘在 `insights.json` 的 `revisit_gate` 里。

**Codex 终审 5 轮**（首轮 9 阻断 + 4 必修，全部成立、无一误报）。三条最贵的：
① 洞察/索引清理失败时 `raise` 会穿到 `run()` 的 finally，把**完好的**清洗产物
一起删掉——2A 就埋着同一个洞；② `corpus_is_first_party=True` 这个无证据默认值
让每次运行都自称自有语料、把条件③永久禁用，等于用一个默认值实现了这条 gate
本来要防的自证；③ 我用「manifest 才是权威」给「留下半份产物」做辩护，却没让
任何读取入口去问 manifest——那是张空头支票，后来按 Codex 的建议改用
provisional manifest，把 `trust_manifest=False` 这个逃生门整个删掉。

**关键词命名的人工验收**（呈现级主题）：Notion 10 组里 9 组可辨认，Apple Notes 5 组里 3 组，
合计 12/15 = 80%，过 70% 线；**但 Apple Notes 单独看是 60%，不达标**——
两个不合格的正是已声明的失效模式「单语言簇会出该语言的通用词」。

## 下一步

**2D 已完成**（2026-08-16）。头号分叉的裁决与它的依据：

开工前先拿两份真实语料的 topic 洞察压成档案看实物，三条发现改变了分叉本身的前提：

1. **不是「五节填得出两节」，是一节。** 所有非 null 的 `claude_md` 都是
   `focus_areas`——2B 从没产过指向「领域词汇表」的洞察。要出第二节只能由 2D
   自己汇总 keywords，那会同时撞「2D 不自己猜路由」与人肉 gate 的存在理由
   （用户审的是一句话，进档案的会是另一句话）。
2. **§6 的「关注领域按稳定性排序」按它自己的定义就填不出来**（时间轴不可用）。
3. **主题只覆盖 kept 的 15.9% / 22.6%**，而 R1 原本不进档案线——档案会让 agent
   把六分之一当成全集。

裁决：**纯管道（方案③）**，节数由上游供料决定，2E 接上自动多三节；
R1 改走档案线补上覆盖率那一节；零可归档条目 fail closed 不写文件。

**Codex 七轮审**（spec 阶段 7 条 + 实现阶段 6 轮，收敛 4→5→4→3→3→3→0，
共 22 条阻断，无一误报）。最值钱的两条：
① 覆盖授权原本只凭一行印在产物里的标记，等于把钥匙印在门上——改为回执 + 内容哈希
+ 拒符号链接 + `O_EXCL` + 锁；② 结构 gate 原本晚于「按勾选过滤」，未勾选的未知
section 有静默通道，且会把「工具不认识这一节」误报成「你一条都没勾」（退出码 8）。

**终审里两条值得记的元教训**：① 修复本身会引入回归——二审为「不留撒谎的回执」
把作废回执提到写档案之前，三审发现它会在磁盘满时要求用户删掉一份完好的档案；
② **一条守不住其声明的测试比没有测试更糟**：三轮里有三条测试被判「守不住它说要守的东西」
（回执 tmp 清理连着两版都没命中要守的那行；「一行都不能多」用集合守；
「导语与语料无关」恒真）。写完断言先问「它有没有可能永远成立」这条，
对**测试自己**同样成立。

**门面验收**：两份真实产物已生成。Notion 那份 10 条主题里多数可辨认；
**Apple Notes 那份前两条是已声明的命名失效模式**（意大利语功能词 / 无信号英语高频词），
处置是 2E 重命名，**不回到 2B 对着这两份语料调参**。

## 2C Wrapped 的三条裁决与它们的依据（2026-08-16）

1. **报告必须印短 ID**。看 `insights.md` 才发现：报告不印 `T1`/`R1`/`C1`，
   用户看完回到清单**找不到对应哪一条**——呈现层与操作层各说各话，
   闭环在人的那一步断掉。这是两层唯一的接缝。
2. **分享版在 compile 生成，不在主 run**。主 run 产报告时用户还没审过任何东西，
   在那时生成分享品 = 把未审内容做成可发出去的文件。这条同时让勾选框的语义
   变完整：勾一条 = 我认可它 = 它可以进档案、也可以被我发出去。
3. **报告逐字显示 `canonical_text`，「好玩」由排版承担**。顺带裁掉 2D 的遗留问题：
   `canonical_text` 不改。文案若与档案不同，用户就是在用 A 句做决定而档案里进的是 B 句。

**只有渲染出来才看得见的一个真问题**：条形原本按全局最大值缩放，碎片区（637 篇）
与语料（757 篇）把尺度吃光，10 个主题（5–29 篇）全挤成看不见的红点。
改为只画主题族、按主题内最大值缩放。**这类问题任何测试都测不出来，只能看。**

**Codex 终审 6 轮**（收敛 4→3→1→1→1→0），10 条阻断成立、1 条不采纳
（它判「分享版写失败后重跑补不上」，而仓库里有一条正在通过的测试证伪）。
最值钱的两条：① 分享版写失败时若留着上一次那份，用户会以为它是最新的就发出去，
而这次取消勾选的条目还在里面；② 「零生成式文案」这条核心合同**一个测试都没有**
——「断言句逐字出现」只证明该在的在了，证不了没有多出来的。

## 发布面（2026-08-17）

**三条前置裁决**（贾老师）：项目名沿用 `kb-init`（PyPI 可用）/ 协议 **Apache-2.0** /
**这一轮只本地备齐，不建公开仓库、不发 PyPI**。

**R15 被实测推翻**：查 PyPI 实际 wheel 清单后确认，`onnxruntime` 至少从 1.18 起
就不发 macOS x86_64 wheel。所以原处方里的「CI 必须验 macOS Intel」不是没验，是
**装不上**——再多 CI 也变不出一个不存在的 wheel。真实支持面已写进 README 与
classifiers：Win x64/arm64 ✅ / Linux（glibc ≥ 2.28）✅ / macOS arm64（≥ 14）✅ /
**macOS Intel ❌**。

**R14 从未实现，现已实现**：`embed.py` 的 progress 钩子躺了很久没人接，
实测索引阶段全程静默而冷启动按分钟计。现在模型准备、向量进度、聚类都会说话
（走 stderr，管道不受影响）；**不报剩余时间**——没有可靠依据的预估就是猜。

### 这一轮的记账（别把「写好了」当成「验过了」）

| 项 | 状态 |
|---|---|
| 依赖侧跨平台 wheel | ✅ 已用 PyPI 清单逐个核实 |
| **构建侧跨平台** | ❌ **未验证**——CI 配置写好了但从没跑过（仓库还没有远端） |
| 打包冒烟 | ✅ 已真跑：`uv build` → 全新 venv 装 wheel → `--version` / `--help` / 一次 `--no-index` 真跑 |
| 公开仓库 / PyPI | ❌ 都没做，按裁决 |

### ⚠️ 公开前的硬门

git 历史里还留着**真实语料内容**（一处社交账号 handle、一处意语学习笔记关键词，
均来自 `f5e9d83`）。工作树已清干净，历史没有。

已装 **pre-push 钩子**在推送那一刻拦下来（`.git/HISTORY-NOT-SCRUBBED` 里写着处置
步骤）——写在这里的提醒靠人记得，而靠人记得等于没做完。

新增 `probes/corpus_leak_probe.py`：探针词从**仓库外**的真实产物读，脚本自身不含
个人数据。它抓到了我肉眼漏掉的两处（2C spec 的示意图里直接写着真实关键词；
2B 计划文档里一个候选，人工判定为误报）。

**这个脚本被 Codex 审了 12 轮、25 条阻断，全部是假绿路径**（真有泄露却报干净）：
git 的 `-i` 对 `-S` 不生效、`--untracked` 不加就看不见新建文件、JSON 的 `\uXXXX`
转义形式、中文没有空格导致片段查不到、空探针集让结论恒为真……这些没有一条
是肉眼能看出来的。**一个会报假绿的检查器比没有检查器更糟**，因为它会在最需要
它的那一次给你绿灯。

### 接下来

**2026-08-16 两条裁决**：

- **2E 降为 v0.2，不进 v0.1。** v0.1 的完成定义是兑现 README 承诺的**两样**产物，
  第二样是 2C 而不是「把档案做厚」；且 v0.1 的传播卖点是「免费预览层」，
  把掏 key 放进 v0.1 会削弱它。代价说清楚：**v0.1 的档案就是两节**。
- **档案措辞问题并进 2C 一起裁。** `canonical_text` 会被 Wrapped 与档案两边渲染，
  到 2C 才看得见它在呈现层里的样子；改一次让两边都对，好过现在盲改。

未了项：
- 「领域词汇表」若要做，正确位置是 **2B 新增一条洞察**（走人肉 gate），不是 2D 汇总。
- 取得一份**第三方语料**后重新评估回头条件③（当前 `not_evaluable`）。
  两份自有语料细分后的 residual 分别是 77.4% / 84.1%，若第三方语料也 >70% 即触发 halo。
- 单语言簇的通用词命名（2B spec §4.4 已记为已知失效模式，兜底是证据行 + 人肉 gate + L3 重命名）。
